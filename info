def parse_join_keys(join_key_str):
    if not isinstance(join_key_str, str):
        join_key_str = str(join_key_str) if not pd.isna(join_key_str) else ''
    return [key.strip() for key in join_key_str.split(',') if key.strip()]