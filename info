# Dynamically resolve script path based on current location (works in .exe too)
base_dir = getattr(sys, '_MEIPASS', os.path.dirname(os.path.abspath(__file__)))
script_path = os.path.join(base_dir, 'dashboard.py')