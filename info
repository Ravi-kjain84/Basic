= Table.AddColumn(#"Replaced Value4", "Updated_SQL", each 
    let
        original = [SQL],
        dotCount = Text.Length(original) - Text.Length(Text.Remove(original, "."))
    in
        if dotCount = 2 then "'" & original & "'" else original
)