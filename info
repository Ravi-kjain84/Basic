wb = excel.Workbooks.Open(str(file.resolve()))

new_file = file.with_suffix('.xlsx')  # This keeps it in the same folder

save_path = str(new_file.resolve())   # Ensures full path

print(f"💾 Saving to: {save_path}")

wb.SaveAs(save_path, FileFormat=51)
wb.Close(SaveChanges=False)