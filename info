IFNULL(
  SAFE_DIVIDE(
    COUNT_DISTINCT(
      IF(ft_creation_flag = 'Created Before FT' AND issue_type = 'Test', Jira_Id, NULL)
    ),
    COUNT_DISTINCT(
      IF(issue_type = 'Test', Jira_Id, NULL)
    )
  ), 0
)