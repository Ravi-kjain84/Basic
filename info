let
    Source = YourPreviousStep,  // replace with your actual previous step name
    GroupedRows = Table.Group(Source, {"Teams"}, {
        {"Leads", each Text.Combine(List.Transform([Leads], Text.From), "#(lf)"), type text}
    })
in
    GroupedRows